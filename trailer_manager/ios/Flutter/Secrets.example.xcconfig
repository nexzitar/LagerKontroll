// iOS secrets template. Copy this file to Secrets.xcconfig and fill in real values.
// Secrets.xcconfig is git-ignored; this template is committed.
// Fed into Info.plist as GMSApiKey via $(MAPS_API_KEY) and read in AppDelegate.swift.
MAPS_API_KEY = your_google_maps_api_key
